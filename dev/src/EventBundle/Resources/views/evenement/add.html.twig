{% for q in Evenements %}
    <h1>L'evenement{{ q.id}}</h1>

    <nb></nb>
    <p>Le type :{{ q.type}}</p>


    <h1>sponseye:</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input class="btn btn-success" type="submit" value="Ajouter" />
    {{ form_end(form) }}


{% endfor %}


